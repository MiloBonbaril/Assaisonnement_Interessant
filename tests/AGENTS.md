# AGENTS

These instructions apply to the `tests/` directory.

## Testing Guidelines
- Use `pytest` for all tests and name test files `test_*.py`.
- Mirror the structure of modules in `src/` when organizing tests.
- Tests must be deterministic and avoid external network or API calls.
- When functionality changes, update or add tests accordingly.

